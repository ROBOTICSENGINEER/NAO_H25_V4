cmake_minimum_required(VERSION 2.8)
project(md1)

find_package(qibuild)

# Create a executable named md1
# with the source file: main.cpp
qi_create_bin(md1 "main.cpp")

# Add a simple test:
enable_testing()
qi_create_test(test_md1 "test.cpp")

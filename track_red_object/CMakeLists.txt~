cmake_minimum_required(VERSION 2.8)
project(walk)

find_package(qibuild)

# Create a executable named walk
# with the source file: main.cpp
qi_create_bin(walk "main.cpp")

# Add a simple test:
enable_testing()
qi_create_test(test_walk "test.cpp")
